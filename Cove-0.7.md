- Terminals survive a restart: quit-and-reopen (or an update) and each terminal tab comes back with its
  scrollback and its shell still running, the screen restored as it last drew.
- A terminal resize keeps exactly one prompt instead of duplicating it.
- ⌘F in a terminal reports a real "n of m", and marks every hit in the buffer.
- ⌘-hovering a path in terminal output shows a pointing hand; the terminal has a modern overlay scrollbar
  that stays clear of the window corner, and its content scrolls under the pane's chrome.
- An unfocused pane's terminal takes the scroll wheel.
- An assistant turn keeps running through an app restart — a mid-turn reply resumes rather than being lost.
- Replies stream with the newest words fading in, at a fraction of the previous rendering cost.
- The assistant reports the backend's live background work — background shells, subagents, workflows — on
  the actions pill.
- The plan-approval and question cards share one chrome, minimize the same way, and their bands are washed
  in the app's own blur.
- Settings shows plan usage per organization.
- A ⌘F find, a jump, or an already-visible target no longer moves the transcript's viewport.
- The transcript rubber-bands, and a short one rests on the bottom of its pane.
- Right-clicking a code block's body opens its menu; force-clicking a word in the transcript or the
  composer looks it up.
- A Source Control row dropped on a pane opens its diff there; a dragged search hit opens centered on the
  match.
- Pane titles lead with their close button and update in the same frame as the content they name.
- A window with no folder open persists across launches.
- The sidebar follows the active tab rather than the pane's last document, and double-clicking its divider
  resets it to the default width.
- The workspace dropdown and the assistant's actions menu open on press, so one drag can pick an item.
- Every assistant sidebar list answers to the file tree's keyboard vocabulary — arrows, ⏎ to rename,
  ⌫ to delete.
- Tabs fade out at the tab bar's leading edge, and the bar no longer scrolls away when another tab closes.
- Step between a message's images in the preview window with ←/→.
- The About window is dressed in the app icon's colors, and Acknowledgements closes with ⌘W.
